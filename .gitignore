# generated by setup.sh (per-stack Dockerfile + compose)
.build/
*.generated.yml
Dockerfile.*.generated

# per-module catkin workspaces under ws/<module>/ — src/ populated by `setup.sh clone`
# (component repos, own git), build/devel local. Whole ws/ gitignored; clone recreates.
/ws/

# NOTE: modules/compute/jax/wheels/*.whl IS tracked (the reused jaxlib artifact —
# avoids the multi-hour jaxlib-from-source rebuild). Intentionally NOT ignored.

# modules/compute/torch/wheels/*.whl is NOT tracked, unlike the jax wheel above —
# it's 274MB, over GitHub's 100MB push limit. Kept host-local
# (/home/ml/risk_aware_assets/wheels_x86/, hardlinked in) instead; the directory's
# README.md IS tracked (see that file for rebuild provenance) so the dir survives a
# fresh clone and the assets: [wheels] bind-mount doesn't break every stack's build.
modules/compute/torch/wheels/*.whl

# ROS / build artifacts
build/
devel/
logs/
Testing/
.catkin_tools/
__pycache__/
*.pyc
*.pyo

# One-off agent/runtime work directories.  Durable conclusions belong in docs/;
# large reproducible evaluations stay host-local next to their runner.
/.tmp_*/
tools/fastlivo/_campaign_*/

# 에이전트/디버깅 작업물 (설계 문서·원자료 덤프). 보존할 가치가 생기면 docs/로 승격.
/scratchpad/

# editor / os
.vscode/
.idea/
.nfs*

# profiling output dumped by jax_main_node_ros_new.py --profiling_* (pkl + png)
/profiling_results/

# rosbags + eval outputs (large; regenerable) — never commit these
*.bag
*.bag.active
/flight_logs/
tools/fastlivo/rerun_eval/*.rrd
tools/fastlivo/rerun_eval/*.tum

# FAST-LIVO tuning: per-config generated yamls + binary parquet are regenerable (configs by
# _tuneN_parallel.sh from the base, parquet by build_master.py). Keep scripts + tune_master.csv.
tools/fastlivo/_t[0-9]*_*.yaml
tools/fastlivo/_vrf*.yaml
tools/fastlivo/_vrfo_*.bag
tools/fastlivo/tune_master.parquet

# per-host override for config/stack.env (see that file's footer)
config/stack.env.local
