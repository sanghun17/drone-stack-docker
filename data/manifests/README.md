# Dataset metadata

Track metadata schemas, label definitions and explicitly publishable references here.
Keep actual private inventories alongside their payloads under `data/results/`.
For reproducibility, an inventory should identify relative payload paths or NAS URIs,
SHA-256, source provenance, finalized labels, generating Git commit and configuration.
NAS-only bags are references, not missing local copies to download automatically.
